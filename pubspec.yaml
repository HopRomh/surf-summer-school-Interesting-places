name: flutterr_app_sum_scho
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.2       # хранение локальных данных
  http: ^1.2.0                     # работа с API
  provider: ^6.1.0                 # управление состоянием
  flutter_svg: ^2.0.9              # SVG-иконки
  google_fonts: ^6.2.0             # шрифты от Google
  intl: ^0.18.1                    # форматирование дат
  cupertino_icons: ^1.0.8          # iOS-иконки

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0            # рекомендации по коду

flutter:
  uses-material-design: true

  assets:
    - assets/icons/     # иконки и асеты
